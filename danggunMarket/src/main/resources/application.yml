spring:
  datasource:
    url: jdbc:mysql://localhost:3306/danggunmarket # ${jdbcURL}
    password: root # ${jdbcPassword}
    username: root # ${jdbcUsername}

  jpa:

    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false

jwt:
  secret:
    key: ${jwtSecretKey}
